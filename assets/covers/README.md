# Cover images

- `chain-gang-cover.jpg` — temporary EPUB/PDF cover (placeholder art).
  Low-ink page with a partial green hash-marked field behind the sticks/chain (sideline view).
- Wired in `build/metadatas/profiles/common.yml` as `cover-image`.
- Replace the JPEG with final cover art when ready; keep the filename/path stable so builds keep working.
